terraform {
  backend "s3" {
    bucket  = "podaac-migration-tf-state"
    key     = "podaac-provider-s3-tagging/${var.stage}/terraform.tfstate"
    region  = "us-west-2"
    profile = "default" # AWS Profile
  }
}